import 'package:flutter/material.dart';

class DeliveryNextScreen extends StatelessWidget {
  const DeliveryNextScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Delivery - Next')),
      body: const Center(child: Text('Next page — implement navigation UI here')),
    );
  }
}
